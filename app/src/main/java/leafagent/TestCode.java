package leafagent;

import leafagent.annotations.Branch;
import leafagent.annotations.Leaf;
import leafagent.info.LeafContainer;

@Branch(desc = "FDFDFDf")
public class TestCode {
    @Leaf
    public static void main(String[] args) {
        new LeafContainer("ddd", "ddd", "ddd");
    }
}